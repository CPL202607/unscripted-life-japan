{
  "name": "unscripted-life-japan",
  "version": "1.0.0",
  "private": true,
  "description": "人生沒有劇本，有時會在日本 — 部落格",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx serve dist",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
